{
  "movie_id": "{{jsonPath request.body '$.movie_id'}}",
  "name": "{{jsonPath request.body '$.name'}}",
  "year": "{{jsonPath request.body '$.year'}}",
  "cast": "{{jsonPath request.body '$.cast'}}",
  "release_date": "{{date parseDate(jsonPath request.body '$.release_date')}}"
}
